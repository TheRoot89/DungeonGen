name: ProjectXXX
main: dungeonGen.DungeonGen
version: 0.1
author: theroot89

commands:
   stop:
      description: Ends the dungeon and deletes the buildings etc.
      usage: /<command>
   instructions:
      description: Displays the WorldEdit workflow.
      usage: /<command>
   start:
      description: Generates a new dungeon in front of the player.
      usage: /<command>